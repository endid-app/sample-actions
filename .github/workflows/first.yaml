name: "Tests"

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  test1:
    name: 'Test 1 dev'
    runs-on: ubuntu-latest

    steps:
      - name: 'First Step'
        run: |
           echo 'Hello!'
